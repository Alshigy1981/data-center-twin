# Core API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Dashboard
streamlit>=1.28.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
httpx>=0.25.0
requests>=2.31.0
pyyaml>=6.0

# Multi-objective optimizer + workload placement MILP
pulp>=2.7.0          # MILP solver for rack assignment (Layer 3)

# Reinforcement learning — gymnasium only (torch is optional, see below)
# NOTE: torch is in requirements.torch.txt — install separately to avoid 400MB Railway build timeout:
#       pip install -r requirements.torch.txt
# The PPO agent falls back to rule-based control if torch/stable-baselines3 unavailable.
gymnasium>=1.0.0            # 1.0 introduced breaking API change (truncated flag, seed kwarg)

# Desktop packaging
pywebview>=4.0.0
pyinstaller>=6.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
playwright>=1.40.0    # browser-driven dashboard verification (run: playwright install chromium)
